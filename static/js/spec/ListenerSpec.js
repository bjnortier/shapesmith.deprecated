describe("Observable", function() {

    var obj;
    beforeEach(function() {
        obj = {};
        Observer.makeObservable(obj);
    });

    it("must specify an event function", function() {
        
    });

});
